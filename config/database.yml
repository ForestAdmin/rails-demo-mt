default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>

test:
  <<: *default
  database: compliance_monitor_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
  prepared_statements: false
